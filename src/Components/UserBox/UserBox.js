import React from 'react';
import './UserBox.css'

const UserBox = (props) => {
    const {id, name, address, email} = props.user;
    return (
        <div>
            
        </div>
    );
};

export default UserBox;